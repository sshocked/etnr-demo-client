apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: etnr-client-ingress
  namespace: etnr-client-service
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  ingressClassName: nginx
  rules:
    # Основной домен — поддомен demo
    - host: demo.lasamb.tw1.ru
      http:
        paths:
          - path: /()(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: etnr-demo-client
                port:
                  number: 80
    # Старый путь /app/ на основном домене (совместимость)
    - host: lasamb.tw1.ru
      http:
        paths:
          - path: /app(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: etnr-demo-client
                port:
                  number: 80
